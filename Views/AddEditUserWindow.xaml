<Window x:Class="ERP.Views.AddEditUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:vm="clr-namespace:ERP.ViewModels"
        Title="Add New Item"
        Height="300" Width="420"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" ResizeMode="NoResize" WindowStyle="None">
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Black"/>
            <GradientStop Color="#FF33487B" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    <Window.DataContext>
        <vm:AddEditUserViewModel />
    </Window.DataContext>

    <Border CornerRadius="8" Padding="20" 
     >
        <Border.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="Black"/>
                <GradientStop Color="#FF2C5182" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题 -->
            <TextBlock Text="➕ Add/Edit User"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="#2C3E50"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <!-- SKU -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8">
                <TextBlock Text="Name:" Width="90" VerticalAlignment="Center" Foreground="#555"/>
                <TextBox Width="240" 
                         Padding="6"
                         BorderBrush="#BDC3C7"
                         BorderThickness="1"
                         Background="#FDFDFD"
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="BorderBrush" Value="#BDC3C7"/>
                            <Setter Property="Background" Value="#FDFDFD"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Style.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="#3498DB"/>
                                    <Setter Property="Background" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </StackPanel>

            <!-- Name -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8">
                <TextBlock Text="Password:" Width="90" VerticalAlignment="Center" Foreground="#555"/>
                <TextBox Width="240" Padding="6"
                         BorderBrush="#BDC3C7"
                         BorderThickness="1"
                         Background="#FDFDFD"
                         Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <!-- Quantity -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8">
                <TextBlock Text="Role:" Width="90" VerticalAlignment="Center" Foreground="#555"/>
                <TextBox Width="240" Padding="6"
                         BorderBrush="#BDC3C7"
                         BorderThickness="1"
                         Background="#FDFDFD"
                         Text="{Binding Role, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <!-- 按钮 -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="Add"
                        Width="90"
                        Margin="0,0,12,0"
                        Padding="6,3"
                        Background="#27AE60"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Command="{Binding ConfirmCommand}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#27AE60"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2ECC71"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Cancel"
                        Width="90"
                        Padding="6,3"
                        Background="#BDC3C7"
                        Foreground="Black"
                        BorderThickness="0"
                        Cursor="Hand"
                        Command="{Binding CancelCommand}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#BDC3C7"/>
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#95A5A6"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>